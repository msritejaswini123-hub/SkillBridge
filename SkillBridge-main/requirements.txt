# SkillBridge - Python dependencies
# Install with:  pip install -r requirements.txt

Flask==3.1.3            # web framework (routing, templates, sessions)
pypdf==6.18.0           # PDF text extraction for resume parsing
gunicorn==23.0.0        # production WSGI server (only needed when deploying)

# ---- Optional: the ML re-ranking layer ----
# The app runs fine WITHOUT these. If scikit-learn is missing, SkillBridge
# automatically falls back to the rule-based recommendation engine only.
scikit-learn==1.9.0
numpy==2.5.3
